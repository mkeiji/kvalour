name: kvalour-ci
on: push

jobs:
    Build:
        runs-on: ubuntu-latest
        container:
            image: google/dart:2.3.2-dev.0.1
        steps:
            - name: Get dependencies
              run: dart --version
